import React from 'react';

const MenteePlannerDetail = ({ params }: { params: { id: string } }) => {
  console.log('params', params);

  return <div>학생 플래너 할일 상세 페이지</div>;
};

export default MenteePlannerDetail;

'use client';

import { useState } from 'react';

const PlannerForm = ({ menteeId }: { menteeId: string }) => {
  return <div className="mx-auto max-w-[600px] p-[24px]">asdfadfs</div>;
};

export default PlannerForm;
